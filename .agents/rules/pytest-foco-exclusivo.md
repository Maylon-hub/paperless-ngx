---
trigger: always_on
---

Sempre que for gerar, sugerir ou refatorar testes automatizados para este projeto, utilize estritamente o framework `pytest`. Não utilize bibliotecas alternativas a menos que o código legado do repositório exija, mas dê preferência absoluta à sintaxe e às fixtures nativas do `pytest` para qualquer novo teste de unidade implementado.
